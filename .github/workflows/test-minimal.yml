name: Test Minimo
on:
  workflow_dispatch:
  push:
    branches: [develop]
jobs:
  prueba:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Workflow funcionando correctamente"
